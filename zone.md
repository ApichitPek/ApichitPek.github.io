# zone 
  + [A portion of the DNS namespace made up of a single domain or of a domain and subdomains that are administered as a single, separate entity.
    A particular name server is responsible for each zone. The name server stores all address mappings for the domain name space with the zone.
    It also answers client queries for those names.]

| User |meaning |
| :--- | :--- |
| **GPT** | ส่วนหนึ่งของพื้นที่ชื่อโดเมน (DNS namespace) ที่ประกอบด้วยโดเมนเดียว หรือโดเมนพร้อมซับโดเมน ซึ่งถูกจัดการเป็นหน่วยแยกอิสระหน่วยเดียวเซิร์ฟเวอร์ชื่อ (Name Server) แต่ละตัวจะรับผิดชอบโซนหนึ่ง ๆ เซิร์ฟเวอร์นี้เก็บข้อมูลการจับคู่ที่อยู่ (address mappings) ทั้งหมดสำหรับพื้นที่ชื่อโดเมนภายในโซนและตอบคำถามจากไคลเอนต์เกี่ยวกับชื่อนั้น ๆ|
| **Gimini** | คือส่วนหนึ่งของพื้นที่ชื่อโดเมน (DNS namespace) ซึ่งประกอบด้วยโดเมนเพียงโดเมนเดียว หรือโดเมนร่วมกับซับโดเมน (subdomains)โดยทั้งหมดนี้จะถูกบริหารจัดการเสมือนเป็นหน่วยเดียวกันที่แยกออกมาต่างหากเนมเซิร์ฟเวอร์ (Name Server) เฉพาะตัวใดตัวหนึ่งจะเป็นผู้รับผิดชอบดูแลแต่ละโซน โดยทำหน้าที่ดังนี้:1.  **จัดเก็บข้อมูล:** เก็บข้อมูลการจับคู่ที่อยู่ (Address Mappings) ทั้งหมดของโดเมนเนมสเปซที่อยู่ภายในโซนนั้น 2.  **ตอบคำถาม:** ทำหน้าที่ตอบกลับคำร้องขอ (Queries) จากไคลเอนต์สำหรับชื่อโดเมนเหล่านั้น|
| **Myself** | พื้นที่ส่วนใดส่วนหนึ่งใน โดเมน โดยมี Name Server ควบคุมอีกทีภายใต้โดเมน หรือเซิร์ฟเวอร์เพื่อให้จัดการดูแลได้ง่ายขึ้น |
